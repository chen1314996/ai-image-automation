# 待办事项与计划 - 2026-05-14

## 今日已完成

- [x] 提交并推送当前分支到远程仓库。
- [x] 确认远程分支 `origin/codex/feishu-cli-baseline` 已同步最新提交。
- [x] 排查创意拓展表格 1925 组与 1475 组数量差异。
- [x] 确认差异来自提示词正文查重，而非表格漏解析。
- [x] 经用户确认后保留提示词查重逻辑。
- [x] 后端新增创意拓展表格解析统计 `parseStats`。
- [x] 前端日志新增“查重前总数、查重后数量、重复数量、重复行示例”提示。
- [x] 增加表格解析查重统计回归测试。
- [x] 用 `冰封末世创意方向拓展.xlsx` 做真实解析验证。
- [x] 排查量产页面无头模式弹出 `chrome-headless-shell.exe` 终端窗口问题。
- [x] 确认弹窗来源为 Playwright 默认 `chromium-headless-shell`。
- [x] 修改 Legil 公共浏览器控制器，使无头模式使用完整 Chromium。
- [x] 验证 `headless + channel: chromium` 不再启动 `chrome-headless-shell.exe`。
- [x] 在不影响当前量产任务的前提下隐藏已弹出的 Windows Terminal 窗口。
- [x] 每次调整后通过飞书机器人发送完成提醒。

---

## 后续待办

### 高优先级

- [ ] **等待当前量产完整工作流结束后重启服务**
  - 让 `playwright-controller.js` 中的 `channel: 'chromium'` 正式加载到正在运行的服务。
  - 重启后再启动一次量产无头模式，确认不会出现 `chrome-headless-shell.exe` 终端窗口。

- [ ] **观察量产无头模式稳定性**
  - 重点观察是否仍有 `Target crashed`。
  - 观察 Legil 页面是否能稳定填入提示词。
  - 观察切换完整 Chromium 后资源占用是否明显变化。

- [ ] **确认创意拓展表格解析日志在页面中展示完整**
  - 上传有重复表格，确认日志显示原始总数和查重后数量。
  - 上传无重复表格，确认日志只显示总数。
  - 确认重复行示例不重复刷同一行对。

### 中优先级

- [ ] **完善提示词查重报告**
  - 如果用户后续需要，可在页面中增加“查看重复详情”入口。
  - 可支持导出重复行对表格，方便用户回到 Excel 清理重复方向。

- [ ] **继续观察量产 Legil 输出检测**
  - 尤其是输出数量为 4 时是否仍出现 3/4 卡住。
  - 确认保存文件命名是否保留简短提示词标题。
  - 确认保存失败时异常截图和飞书通知是否正常。

- [ ] **服务重启时机管理**
  - 当有任务正在运行时，继续避免强制重启。
  - 在页面或飞书卡片中提示“有任务运行中，建议结束后重启服务”。

### 低优先级

- [ ] **整理创意拓展表格规范文档**
  - 推荐列名。
  - 查重规则。
  - 重复提示词处理逻辑。
  - 日志说明。

- [ ] **补充无头模式技术说明**
  - Playwright `chromium-headless-shell` 与完整 Chromium 的差异。
  - 为什么 Windows 上可能出现终端窗口。
  - 当前项目采用 `channel: 'chromium'` 的原因。

---

## 明日计划

1. 在量产任务停止后重启服务，使无头模式浏览器启动修复正式生效。
2. 用量产页面完整工作流再做一次无头模式启动验证。
3. 上传用户表格回归验证创意拓展解析日志。
4. 继续观察 Legil `Target crashed` 是否仍出现。
5. 视情况提交并推送当天修复。

---

*记录时间: 2026-05-14，2026-05-15 补充整理*
