# 待办事项与计划 - 2026-05-29

## 今日已完成

- [x] 新增 `docs/S10-量产与三尺寸交付后处理新版分阶段开发方案-20260529.md`。
- [x] 明确 S10 两段流水线：自动创意量产与 OK 图三尺寸交付。
- [x] 明确 OK 图人工复制到交付输入文件夹，不移动、不破坏原量产目录。
- [x] 明确三尺寸目标：`800x800`、`1280x720`、`1080x1920`。
- [x] 明确三尺寸适配主流程只保留 Legil。
- [x] 新增主导航“三尺寸交付”入口。
- [x] 新增三尺寸交付页面结构。
- [x] 新增 `public/js/delivery-page.js`。
- [x] 新增三尺寸交付页面样式。
- [x] 新增 `src/services/delivery-postprocess/store.js`。
- [x] 新增 `src/services/delivery-postprocess/candidate-config.js`。
- [x] 新增 `src/services/delivery-postprocess/standardize.js`。
- [x] 新增 `src/routes/delivery.routes.js` 并注册到服务路由。
- [x] 接入 delivery scan、start、resume、stop、retry-target、select-candidate、standardize、finalize、status、runs、image 等接口。
- [x] 新增 `src/services/postprocess-trace.js`。
- [x] 新增 `scripts/test-s10-postprocess-trace.js`。
- [x] 新增 `scripts/test-s10-delivery-candidates.js`。
- [x] 注册 `npm run test:s10-postprocess`。
- [x] 继续修补创意自动化、Prompt Gate、素材分析视觉任务和创意拓展提示词池相关逻辑。

---

## 后续待办

### 高优先级

- [ ] **补齐三尺寸交付接口文档**
  - 在 `docs/接口清单.md` 新增“三尺寸交付”章节。
  - 写入 `/api/delivery/*` 全部接口。
  - 标明当前 `finalize` 仍为占位。

- [ ] **真实 Legil headed 验收 S10.3 候选生成**
  - 准备 1-2 张 OK 图。
  - 扫描 OK 图输入文件夹。
  - 启动三尺寸候选生成。
  - 验证每个 target 的候选数量、保存路径、状态更新。
  - 验证停止和继续。
  - 验证重跑单 target。

- [ ] **补齐 LOGO 叠加**
  - 三个尺寸分别读取 LOGO 模板。
  - 明确覆盖方式：原样覆盖、按模板尺寸覆盖，或按比例缩放。
  - 将结果写入 target 的 `logoPath` 或 `finalPath`。

- [ ] **补齐最终重命名和按图打包**
  - 每张 OK 图一个文件夹。
  - 文件夹名不带尺寸后缀。
  - 图片名带尺寸后缀。
  - 生成 `manifest.json` 和 `batch-manifest.json`。

### 中优先级

- [ ] **完善 delivery 页面运行态**
  - 任务运行中禁用不该点的按钮。
  - 停止后明确显示可继续。
  - 失败 target 可以单独重跑。
  - 候选缩略图点击可预览大图。

- [ ] **处理旧改尺寸入口降级**
  - 从主导航隐藏旧改尺寸。
  - 保留旧 resize 能力作为内部工具或兜底工具。
  - 配置页文案改为“AI 三尺寸适配参数”。

- [ ] **完善标准化批量处理**
  - 当前测试覆盖 800x800。
  - 后续补 1280x720 和 1080x1920 的批量标准化脚本验证。
  - 标准化失败时要在 target 上保留错误信息。

- [ ] **补充 delivery run 清理策略**
  - `data/delivery-postprocess/` 当前是运行产物。
  - 后续确认是否需要加入 `.gitignore`，避免真实交付记录误提交。

### 低优先级

- [ ] **增强交付报告**
  - run 完成后生成交付摘要。
  - 输出每张 OK 图的三尺寸完成状态、失败原因、最终路径。

- [ ] **把三尺寸交付接入飞书通知**
  - 完成、失败、暂停时发送摘要。
  - 避免每个 target 都刷屏。

- [ ] **拆分提交**
  - 建议单独提交 S10 文档、delivery 数据层、delivery 路由、delivery 页面、标准化和测试脚本。

---

## 明日计划

1. 补齐 `docs/接口清单.md` 中 `/api/delivery/*`。
2. 用真实 Legil 跑 1 张 OK 图的三尺寸候选生成，记录输出路径和候选状态。
3. 完善候选预览与选中体验。
4. 接入或设计 LOGO 叠加步骤。
5. 将 `data/delivery-postprocess/` 归入运行产物忽略策略。

---

*记录时间: 2026-05-29；本文件由 2026-06-01 根据项目文件和验证脚本补写。*
