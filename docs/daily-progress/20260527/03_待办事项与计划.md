# 待办事项与计划 - 2026-05-27

## 今日已完成

- [x] 新增 LLM Provider Rule，规定新/改 LLM 能力统一使用 Lumos Winky。
- [x] 将历史 `doubao-automation.js` 改成 Lumos Winky 兼容适配器。
- [x] 保留 `/api/doubao/*` 历史接口名，避免破坏前端和工作流调用。
- [x] 更新提示词生成服务，使旧 `doubao`、`ark`、`volcengine` provider 输入归一到 `lumos`。
- [x] 更新配置接口、前端配置文案、浏览器文案、飞书状态、启动 banner 和 showcase 文案。
- [x] 新增创意知识库服务目录 `src/services/creative-knowledge/`。
- [x] 新增创意知识库 API 路由 `src/routes/creative-knowledge.routes.js`。
- [x] 新增创意知识库前端脚本 `public/js/creative-knowledge.js`。
- [x] 实现方向、参考图、TOP 素材、资产、反馈、运行记录、方向草稿和记忆规则的本地 JSON 存储。
- [x] 新增运行一次自动创意服务目录 `src/services/creative-auto/`。
- [x] 实现自动选题、Prompt Gate、Prompt 转译、资产入库和从 Prompt Gate 继续 Legil。
- [x] 新增自动创意 API 路由 `src/routes/creative-auto.routes.js`。
- [x] 新增创意拓展页 S3 工作台、运行历史、Prompt Gate 面板和方向选择 UI。
- [x] 新增素材分析服务目录 `src/services/material-analysis/`。
- [x] 实现素材 CSV/XLSX 导入、Top100、健康度、方向表现、视觉识别、报告和创意 brief。
- [x] 新增素材分析 API 路由 `src/routes/material-analysis.routes.js`。
- [x] 新增素材分析前端脚本和样式。
- [x] 在主页面导航中接入 `知识库` 和 `素材分析` 页面。
- [x] 改尺寸任务新增图片 x 比例 job 模型。
- [x] Legil 改尺寸新增恢复状态读取、清除、继续任务。
- [x] 即梦改尺寸支持多比例顺序处理和续跑状态写入。
- [x] 前端改尺寸页新增继续任务、新任务和恢复提示。
- [x] 新增 Agent `PROJECT_SETTINGS.md`。
- [x] 新增 `legil-run-once-prompt-contract` skill。
- [x] 更新 Agent `README.md`、`agent.yaml`、`instructions.md` 和 TypeScript integration。
- [x] 新增或更新自动创意、24 小时闭环、素材分析、S0 基线等方案文档。
- [x] `package.json` 增加 S0-S6、素材分析、Prompt Gate、创意知识库等测试脚本。
- [x] 执行 `node --check` 检查 147 个 JS 文件，通过。
- [x] 执行今日新增/相关 npm 回归脚本，全部通过。
- [x] 按 `docs/daily-progress` 格式生成 2026-05-27 日报。

---

## 后续待办

### 高优先级

- [ ] **决定本地数据提交策略**
  - 检查 `data/`、`sucai/`、验收截图、临时 Excel 是否应该提交。
  - 如果它们只是运行产物，补充 `.gitignore`。
  - 如果需要作为样例数据，整理命名和说明文档后再提交。

- [ ] **补齐素材分析接口文档**
  - `docs/接口清单.md` 已记录创意知识库和运行一次自动创意接口。
  - 还需要补充 `/api/material-analysis/*` 全部接口。
  - 同时确认 `/api/creative-auto/runs/:runId/start-legil` 是否需要写入接口清单。

- [ ] **真实验证 Lumos Winky 图文能力**
  - 用真实参考图跑一次 `/api/doubao/full-automation` 兼容接口。
  - 用真实素材图跑一次素材分析视觉识别。
  - 验证鉴权错误、限流、模型返回非 JSON、超时等错误提示是否清楚。

- [ ] **真实验证“运行一次自动创意”**
  - 先用 `agentOnly` 模式生成 prompt。
  - 人工检查 Prompt Gate 输出。
  - 再从 Prompt Gate 继续 Legil。
  - 确认图片保存、资产入库、运行历史和方向统计都正确。

- [ ] **真实验证改尺寸续跑**
  - Legil 改尺寸：选择多个比例，中途停止，再点击继续任务。
  - 即梦改尺寸：选择多个比例，中途停止，再点击继续任务。
  - 确认 `nextIndex`、已保存数量、剩余组数和前端按钮状态一致。

### 中优先级

- [ ] **浏览器视觉验收新页面**
  - 打开知识库页，确认方向树、资产详情、反馈面板不遮挡。
  - 打开创意拓展页 S3 工作台，确认多选方向、Prompt Gate 面板和运行历史布局正常。
  - 打开素材分析页，确认 Top100、视觉墙、报告和 brief 区块在桌面/移动端都可用。

- [ ] **完善创意知识库导入体验**
  - 检查 `sucai` 路径不存在或表格缺字段时的提示。
  - 检查参考图抽取失败时是否可以继续导入方向。
  - 为导入结果增加更清晰的 warnings 展示。

- [ ] **完善素材分析报告**
  - 将素材分析生成的周报、经验文档和创意 brief 与知识库方向证据打通。
  - 对方向表现加更多跨周字段预留。
  - 明确 Top100 健康度阈值是否需要按项目调参。

- [ ] **补充飞书通知策略验收**
  - 自动创意完成、暂停、失败时是否按策略发通知。
  - agentOnly 模式是否正确 suppress 不必要通知。
  - 连续失败暂停是否能出清晰提示。

- [ ] **整理提交拆分**
  - 建议不要一次性把所有改动压成一个提交。
  - 可按 Winky 迁移、创意知识库、运行一次自动创意、素材分析、改尺寸续跑、Agent 文档和测试脚本拆分。

### 低优先级

- [ ] **统一历史“豆包”命名**
  - 当前为了兼容保留 `/api/doubao/*` 和部分变量名。
  - 后续可以新增更中性的 `/api/prompt-generation/*`，再逐步迁移前端。

- [ ] **整理测试样例数据**
  - `test-material-analysis-parser.js` 当前依赖 Downloads 中的特定 CSV。
  - 后续可以把脱敏小样例放入 `tests/fixtures/`，减少环境依赖。

- [ ] **补充端到端验收脚本**
  - 当前多数测试是服务级和契约级。
  - 后续可增加 Playwright 页面验收，检查按钮、列表、面板和图片预览。

- [ ] **完善 24 小时循环前置条件**
  - 单轮 run-once 稳定后，再加调度器。
  - 调度器需要接入每日额度、连续失败暂停、登录状态检测、人工审核阈值和反馈学习规则。

---

## 明日计划

1. 先清点 `data/` 和 `sucai/`，决定提交/忽略策略，避免把大量本地运行产物误提交。
2. 补齐 `docs/接口清单.md` 的素材分析接口和 run-once start-legil 接口。
3. 用真实 Winky 配置跑一次提示词生成和素材视觉识别。
4. 用真实 Legil headed 模式跑一次 agentOnly -> Prompt Gate -> 继续 Legil -> 资产入库。
5. 对知识库、创意拓展 S3、素材分析三个页面做浏览器截图验收。
6. 视验收结果决定是否拆分提交。

---

*记录时间: 2026-05-27*
