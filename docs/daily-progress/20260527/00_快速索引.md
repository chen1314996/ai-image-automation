# 快速索引 - 2026-05-27

## 今日核心成果

- 将提示词生成和视觉识别方向统一到 **Lumos Winky**：历史 `/api/doubao/*` 路由继续保留兼容名，但实现和文案都转向 Winky，不再保留火山/方舟/豆包模型作为新功能入口。
- 新增“创意知识库”基础能力：可导入 `sucai` 下的方向种子表、TOP 素材和参考图索引，并提供方向、资产、反馈、运行记录、方向草稿、记忆规则等本地 JSON 存储和 API。
- 新增“运行一次自动创意”第一版闭环：知识库选题、Agent 拓展、Prompt Gate、Prompt 转译、Legil 继续生图、资产入库、运行历史、每日额度和 preflight 检查。
- 新增“素材分析”页面和服务：支持 CSV/XLSX 导入、Top100 花费排序、健康度判断、方向表现、AI 视觉识别、周报/经验文档、创意 brief 与创意拓展联动。
- 改尺寸流程补充多比例任务队列和续跑能力：Legil 与即梦改尺寸都改为“图片 x 比例”的 job 模型，支持中断后继续剩余任务。
- 更新创意拓展 Agent 包：新增 `PROJECT_SETTINGS.md` 和 `legil-run-once-prompt-contract` skill，将 Nano Banana 2、1:1、2K、每 prompt 4 张等约束写入 Agent 合约。
- 补充阶段计划、接口清单和回归脚本，形成 S0-S6、素材分析、Prompt Gate、创意知识库、自动创意、反馈学习等测试入口。

## 关键文件位置

| 文件/目录 | 说明 |
| --- | --- |
| `AGENTS.md` | 新增 LLM Provider Rule，规定新/改 LLM 能力必须走 Lumos Winky。 |
| `doubao-automation.js` | 历史豆包适配器改为 Winky 图文模型调用，保留旧 API 名称兼容前端和工作流。 |
| `prompt-generation-service.js` | 提示词生成服务默认 provider 改为 `lumos`，保留旧 provider 输入但归一到 Winky。 |
| `src/services/creative-knowledge/` | 创意知识库导入、方向、资产、反馈、方向草稿、记忆学习、本地存储。 |
| `src/routes/creative-knowledge.routes.js` | 创意知识库 API。 |
| `public/js/creative-knowledge.js` | 创意知识库前端页面逻辑。 |
| `src/services/creative-auto/` | 运行一次自动创意：选题、Prompt Gate、Prompt 转译、资产入库、Legil 续跑。 |
| `src/routes/creative-auto.routes.js` | 自动创意状态、选题建议、run-once、从 Prompt Gate 继续 Legil 的 API。 |
| `public/js/creative-auto.js` | 创意拓展页 S3 工作台、方向选择、运行历史、Prompt Gate 面板。 |
| `src/services/material-analysis/` | 素材数据导入、健康度、报告、视觉识别、创意 brief 服务。 |
| `src/routes/material-analysis.routes.js` | 素材分析 API。 |
| `public/js/material-analysis.js` | 素材分析前端页面逻辑。 |
| `public/css/material-analysis.css` | 素材分析页面样式。 |
| `src/server/context.js` | 新增改尺寸续跑状态、图片 x 比例 job 构建、resume state 持久化。 |
| `src/routes/legil.routes.js` | Legil 改尺寸续跑、多比例 job、创意批量生成等接口增强。 |
| `src/routes/jimeng.routes.js` | 即梦改尺寸续跑和多比例任务支持。 |
| `src/services/jimeng-browser/index.js` | 即梦按图片和比例顺序处理，并写入可恢复进度。 |
| `public/js/resize-workflow.js` | 改尺寸页面新增继续任务、新任务、多比例摘要和恢复状态 UI。 |
| `agents/creative-expansion-agent/PROJECT_SETTINGS.md` | 当前项目的 run-once MVP 参数和同步规则。 |
| `agents/creative-expansion-agent/skills/legil-run-once-prompt-contract/` | 新增 Agent skill，约束可被程序解析并可直接送 Legil 的 prompt 表。 |
| `scripts/test-*.js` | 新增 S0-S6、素材分析、自动创意、知识库、Prompt Gate 等回归脚本。 |
| `docs/daily-progress/20260527/` | 今日日报文档。 |

## 当前结论

- 当前分支是 `codex/feishu-cli-baseline`，最新提交仍是 `58ad669 Improve automation workflows and Feishu controls`，今日工作尚未提交。
- 当前工作区有大量未提交改动：`git diff --shortstat` 显示 40 个已跟踪文件变更，约 4565 行新增、860 行删除；另有多个新增服务、前端脚本、测试脚本、文档和本地数据目录。
- Lumos Winky 统一入口、创意知识库、运行一次自动创意、素材分析、改尺寸续跑这几条主线已经进入可回归测试状态。
- 今日补跑的本地验证全部通过，但真实浏览器长流程、真实 Winky 视觉识别、真实 Legil 大批量生成仍建议单独验收。

## 验证摘要

```text
node --check 147 个 JS 文件
npm run test:creative-auto
npm run test:s1-knowledge
npm run test:s2-feedback
npm run test:s3-creative-page
npm run test:s4-growth
npm run test:s5-prompt-translator
npm run test:s6-feedback-learning
npm run test:p5-creative-brief
npm run test:material-analysis
npm run test:s0-baseline
```

以上命令均通过。

## 后续重点

- 决定 `data/`、`sucai/`、验收截图和素材样例是否要提交，或进一步加入 `.gitignore`。
- 用真实 Winky 配置和真实素材图跑一次素材分析视觉识别，确认鉴权、限流、缓存和失败重试行为。
- 用真实 Legil headed 模式跑一次多 prompt 的“运行一次自动创意”，确认保存图片、资产入库、运行历史、继续 Legil 都符合预期。
- 补齐 `docs/接口清单.md` 中素材分析相关接口，避免接口文档落后于新路由。
- 对创意知识库页、自动创意页、素材分析页做一次浏览器视觉验收，确认移动端和桌面端布局不挤压、不遮挡。

---

*记录时间: 2026-05-27*
