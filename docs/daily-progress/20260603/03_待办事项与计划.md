# 待办事项与计划 - 2026-06-03

## 高优先级

- 用真实三尺寸交付目录跑一次完整链路：

```text
候选选择 -> 标准化 -> finalize -> final-package
```

- 验证三类 LOGO 模板：

```text
800x800
1280x720
1080x1920
```

- 确认最终 JPG 输出满足：
  - 尺寸精确。
  - LOGO 正确叠加。
  - 文件体积达标。
  - 文件名符合 GOFCNIM + 业务标签 + 尺寸规则。
- 给 `scripts/fix-creative-output-names.js` 写使用说明和风险提示，明确必须先 dry-run。
- 复盘今日 107 条 creative run，重点看 paused、agent_cancelled 和 running 状态。
- 检查所有新增/修改 LLM 调用点是否都只走 Lumos Winky。

## 中优先级

- 把命名修复报告的核心统计展示到前端或管理文档中。
- 为 `data/rename-reports/` 增加归档策略。
- 给 S10 final-package 增加历史 run 查看和失败 target 重跑入口。
- 将 Creative Agent 的 JSON contract 写入 Agent 文档和接口说明。
- 把 `candidateDirections` 到 Prompt Gate 的字段映射补充成测试。
- 扩展 `test:creative-output-naming`，覆盖 `source-business-name.js` 的更多真实文件名样本。

## 低优先级

- 将今日命名修复作为一次迁移记录写进长期维护文档。
- 后续将 Excel 导出从主流程移到“人工查看/下载”功能中。
- 设计 creative run 自动归档策略，例如按日期、状态、是否入库分组。
- 为 final-package 增加一键打开输出目录或复制路径。

## 下一步建议顺序

```text
1. 先验收 S10 finalize 的真实三尺寸交付。
2. 再补命名修复脚本文档和更多测试样本。
3. 然后清理自动创意运行产物和异常 run。
4. 最后继续把 Agent JSON contract 接到 Prompt Gate 和统一 Legil 队列。
```

## 风险提醒

- `fix-creative-output-names.js --apply` 会真实改文件名，必须先 dry-run 并确认 targetDir。
- JSON 同步会触碰大量 run 文件，后续如果要提交，需要明确运行产物提交策略。
- `automation-secrets.json` 今日有修改时间，日报不记录密钥内容，也不应提交密钥。
- S10 finalize 虽然已具备代码能力，但还需要真实图片和 LOGO 模板验收后才能视为交付闭环完成。

---

*记录时间: 2026-06-03；本文件根据今日项目文件、git 状态、重命名报告和运行产物补写。*
