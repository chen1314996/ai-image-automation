# 今日工作总结 - 2026-06-03

## 已完成的关键工作

### 1. 创意输出命名标准化继续收口

**目标**

解决历史创意输出文件名中技术前缀、版本号、日期时间和业务标签混杂的问题，让最终资产命名更接近交付需要。

**完成内容**

- 新增 `src/services/output-naming/source-business-name.js`。
- 支持从两类文件名中提取业务命名段：
  - Legil 自动生成图文件名。
  - GOFCNIM 交付命名文件名。
- 识别并清理以下技术段：

```text
尺寸：800x800 / 1280x720 / 1080x1920
版本：v01 / v02 / ...
日期时间：20260603 / 104609
序号：0001 / 0016 / ...
GOFCNIM 前缀
```

- `src/services/image-renamer.js` 调整为优先复用解析出的业务名，再拼接 GOFCNIM、区域、渠道和尺寸。
- `scripts/test-s10-delivery-candidates.js` 补充断言：delivery baseName 应继承生成图业务命名，并丢弃生成过程元数据。

---

### 2. 历史创意输出批量修复脚本落地

**目标**

对已经生成的大量历史素材做一次命名迁移，并同步修正资产库和 run JSON 中的文件名引用。

**完成内容**

- 新增 `scripts/fix-creative-output-names.js`。
- 支持：
  - `--dir` 指定目标目录。
  - 默认 dry-run。
  - `--apply` 实际重命名。
  - `--no-sync-json` 禁止同步 JSON。
  - `--include-legacy` 清理旧 ref/prompt 命名。
  - `--legacy-only` 只处理旧技术名前缀。
- 防护：
  - 所有 rename 路径必须在目标目录内。
  - 先生成映射，再去重校验。
  - 避免目标文件冲突。
  - 使用临时文件名两段式 rename，降低大小写/同名冲突风险。
- JSON 同步：
  - 更新 `data/creative-knowledge/assets.json`。
  - 扫描并更新 `data/creative-knowledge/runs/*.json`。
  - 同步更新 label metadata、`standardLabelPath`、`primaryTag`、`secondaryTag`、`tertiaryTag`、`sourceDirectionPath` 等字段。

**运行结果**

```text
creative-output-rename-20260603_034016.json  dry-run  2097
creative-output-rename-20260603_034239.json  dry-run  2097
creative-output-rename-20260603_034414.json  apply    2097
creative-output-rename-20260603_034514.json  dry-run  18
creative-output-rename-20260603_034600.json  dry-run  16
creative-output-rename-20260603_034607.json  apply    16
creative-output-rename-20260603_034626.json  dry-run  0
```

最终 dry-run 为 0，说明本轮目标目录内可识别的待修复项已经清完。

---

### 3. S10 三尺寸交付进入 finalize 阶段

**目标**

把 S10 从候选生成、人工选图、标准化推进到最终交付包阶段。

**完成内容**

- 完善 `src/services/delivery-postprocess/finalize.js`。
- 支持根据目标尺寸自动匹配 LOGO 模板：

```text
800x800
1280x720
1080x1920
```

- 校验 LOGO 模板必须是 PNG，且尺寸与目标尺寸一致。
- 将标准化候选图与 LOGO 模板叠加。
- 输出 JPG，并继续控制体积。
- 输出目录进入：

```text
<outputFolder>/<runId>/final-package/<jobFolder>/
```

- `src/services/delivery-postprocess/store.js` 和 `public/js/delivery-page.js` 继续适配 baseName、folderName 和命名规则。
- `data/delivery-postprocess/runs/delivery_run_20260529_211531_fa5c9f6a.json` 今日继续更新。

---

### 4. 后处理追溯展示增强

**目标**

让用户在重命名、改尺寸、加 LOGO、打包等页面操作后，能看到本次操作是否产生了可追溯派生资产。

**完成内容**

- 修改 `public/js/rename-images.js`。
- 在以下流程结果统计中增加追溯数量：
  - 批量重命名。
  - 批量改尺寸。
  - 批量加 LOGO。
  - 批量打包。
- 示例命名也调整为更贴近业务标签结构：

```text
GOFCNIM28930_BJ_广点通_题材_探索发现_三级标签_细分命名_800x800.png
```

---

### 5. Creative Agent JSON-first 调整

**目标**

让 Creative Agent 不再依赖 Markdown/Excel 表格作为主输出契约，而是直接输出可被程序解析、进入 Prompt Gate 和 Legil 的 JSON。

**完成内容**

- `creative-agent-service.js` 加载新增配置：

```text
agents/creative-expansion-agent/PROJECT_SETTINGS.md
agents/creative-expansion-agent/CREATIVE_PROMPT_AGENT_SETTINGS.md
agents/creative-expansion-agent/skills/legil-run-once-prompt-contract/SKILL.md
```

- 输出契约改为 `candidateDirections`。
- 每个候选方向包含：

```text
type
sourcePath
targetLevel
label
description
dimensions
duplicateRisk
reason
prompts
```

- `dimensions` 至少覆盖：

```text
氛围
视角
时间天气
叙事动作
规模
材质质感
主体关系
广告钩子
```

- 增加从 JSON 中抽取 candidateDirections 和 prompts 的解析逻辑。
- structured batch 从 `structured-table-batch` 调整为 `structured-prompt-batch`。
- 保留 Excel/workbook 生成能力作为展示/兼容层，不再作为主运行契约。

---

### 6. LLM 调用统一与稳定性补强

**目标**

继续落实 AGENTS.md 中的规则：新改 LLM 能力必须走 Lumos Winky。

**完成内容**

- `creative-agent-service.js` 增加：
  - 网络错误可重试判断。
  - `postCreativeAgentApi()` 二次重试。
  - 错误信息脱敏。
  - AggregateError 子错误展开。
  - 对 `gpt-5.5` 类模型使用 `max_completion_tokens`，其他模型继续用 `temperature` + `max_tokens`。
- 以下模块今天有 LLM 网关相关调整：

```text
src/services/material-analysis/vision/vision-client.js
src/services/task-workbook/vision-client.js
src/services/creative-auto/prompt-translator.js
src/services/creative-knowledge/feedback-learning.js
src/services/material-analysis/creative-expansion.js
creative-agent-service.js
```

---

### 7. 自动创意持续跑量

**目标**

验证自动创意、Prompt Gate、Legil 和资产登记在较高频率下的持续运行能力。

**完成内容**

- 今日新增 107 条 `data/creative-knowledge/runs/creative_run_20260603_*.json`。
- 状态分布：

```text
completed / legil_completed：101
paused / legil_paused：2
completed / agent_completed：2
paused / agent_cancelled：1
running / legil_running：1
```

- `data/creative-knowledge/assets.json`、`creative-target-queues.json`、`scheduler-state.json` 今日继续更新。

## 今日验证记录

本次日报补写时未重新执行完整 npm 回归。

已复核的本地结果：

```text
重命名 apply：2097 + 16
最终 dry-run：0
今日 creative run：107
今日 legil_completed：101
```

## 当前阶段结论

今天把“生成资产”之后的命名、追溯、最终交付和 Agent 输出契约都往前推了一截。最关键的变化是：资产命名开始向业务标签收束，S10 finalize 开始进入真实最终交付，Creative Agent 开始 JSON-first。下一步要用真实交付目录做完整验收，并给高频自动创意运行产物建立归档和异常复盘机制。

---

*记录时间: 2026-06-03；本文件根据今日项目文件、git 状态、重命名报告和运行产物补写。*
