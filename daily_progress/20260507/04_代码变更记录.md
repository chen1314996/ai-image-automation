# 代码变更记录 - 2026-05-07

## 今日变更概览

| 文件 | 变更类型 | 变更次数 | 主要修改 |
|------|----------|----------|----------|
| `legil-automation.js` | 修改 | 4次 | 图片保存逻辑重构 |
| `doubao-automation.js` | 修改 | 1次 | 提示词提取优化 |
| `CLAUDE.md` | 新增 | 1次 | 项目文档创建 |

---

## 详细变更记录

### 1. legil-automation.js

#### 变更1: 新增图片生成检测逻辑 (22:00)
**修改内容:**
```javascript
async waitForImageGeneration(page) {
    // 最大等待时间：300秒（5分钟）
    const maxWaitTime = 300000;
    
    // 第一步：等待"生成中..."出现
    let generatingStarted = false;
    while (!generatingStarted && generatingCheckCount < 30) {
        const generatingText = await page.$('text=生成中');
        if (generatingText && await generatingText.isVisible()) {
            generatingStarted = true;
            break;
        }
    }
    
    // 第二步：等待"生成中..."消失
    while (waited < maxWaitTime) {
        const generatingText = await page.$('text=生成中');
        if (!generatingText) {
            logger.info('✅ "生成中..."已消失，图片生成完成');
            return true;
        }
    }
}
```

**原因:** 解决生成检测过早触发问题

---

#### 变更2: 重构图片保存逻辑 (22:30-22:58, 共3次迭代)

**迭代1: 拼接完整URL (22:30)**
```javascript
// 处理相对路径
if (imgSrc.startsWith('/')) {
    const pageUrl = page.url();
    const baseUrl = new URL(pageUrl).origin;
    fullUrl = baseUrl + imgSrc;
}
```

**迭代2: 增加截图备选方案 (22:45)**
```javascript
// 方法2: 使用 Playwright 截图功能
const box = await imgElement.boundingBox();
if (box && box.width > 100 && box.height > 100) {
    await imgElement.screenshot({ path: savePath });
}
```

**迭代3: 简化保存流程 (23:00)**
```javascript
async saveGeneratedImage(page, index, generationStartTime) {
    // 找到最新生成的图片
    let imgElement = await this.findLatestGeneratedImage(page);
    if (!imgElement) {
        throw new Error('无法找到生成的图片');
    }
    
    // 方法1：先尝试截图（最可靠的方式）
    try {
        const box = await imgElement.boundingBox();
        if (box && box.width > 200 && box.height > 200) {
            await imgElement.screenshot({ path: savePath });
            logger.info(`✅ 图片已截图保存: ${fileName}`);
            return savePath;
        }
    } catch (screenshotError) {
        logger.warn(`截图失败: ${screenshotError.message}`);
    }
    
    // 方法2：直接下载
    return await this.downloadImageDirectly(page, imgElement, savePath);
}
```

**废弃代码:**
- 移除了右键菜单点击逻辑（不稳定）
- 移除了 CDP Session 下载行为设置
- 移除了 Promise 包装器等待下载事件

---

### 2. doubao-automation.js

#### 变更: 优化提示词提取算法 (23:00)

**修改内容:**
```javascript
// 原正则（不支持空格）
/groupPatterns = [
    /第[一二三四五12345]组[:：]\s*([\s\S]*?)(?=第[一二三四五12345]组[:：]|$)/g,
]

// 新正则（支持空格）
/groupPattern = /第\s*([一二三四五12345])\s*组[：:]?\s*([^\n]*)([\s\S]*?)(?=第\s*[一二三四五12345]\s*组[：:]|$)/g
```

**新增代码块提取:**
```javascript
// 提取代码块中的内容
const codeBlockMatch = content.match(/```(?:plaintext)?\s*([\s\S]*?)```/);
if (codeBlockMatch) {
    content = codeBlockMatch[1].trim();
}

// 清理冗余文本
fullPrompt = fullPrompt
    .replace(/plaintext/g, '')
    .replace(/复制/g, '')
    .trim();
```

**废弃代码:**
- 移除了旧的 `groupPatterns` 数组方式
- 移除了按段落分割的备选方法

---

### 3. CLAUDE.md (新增)

**创建时间:** 23:05

**内容:**
- 项目架构说明
- API 端点列表
- 核心文件职责表
- Playwright 常用选择器参考
- 故障排查指南

---

## API 变更说明

### 无破坏性变更

今日所有变更均为内部实现优化，API 接口保持不变：

| 端点 | 状态 | 说明 |
|------|------|------|
| `POST /api/legil/generate` | ✅ 稳定 | 内部优化，接口不变 |
| `POST /api/legil/batch-generate` | ✅ 稳定 | 内部优化，接口不变 |
| `POST /api/doubao/full-automation` | ✅ 稳定 | 内部优化，接口不变 |

---

## Git Commit 建议

### 建议提交信息

```bash
# 如果今天只做一次提交
git add .
git commit -m "feat: 完成 Legil 自动化并优化图片保存

- 实现 Legil 平台完整自动化流程
- 优化图片生成检测（等待'生成中'状态）
- 重构图片保存逻辑（截图+下载双保险）
- 优化豆包提示词提取（支持代码块格式）
- 创建 CLAUDE.md 项目文档"

# 如果分多次提交
git add legil-automation.js
git commit -m "feat: 优化 Legil 图片保存逻辑

- 添加截图作为首选保存方式
- 支持相对URL路径下载
- 移除不稳定的右键菜单点击"

git add doubao-automation.js
git commit -m "fix: 优化豆包提示词提取

- 支持带空格的'第 X 组'格式
- 支持从代码块提取内容
- 合并画面内容和风格要求"

git add CLAUDE.md
git commit -m "docs: 创建项目文档"
```

### 变更统计
```bash
# 今日代码统计
git diff --stat HEAD  # 查看变更文件数
wc -l legil-automation.js doubao-automation.js  # 代码行数
```

---

## 测试记录

| 测试项 | 结果 | 备注 |
|--------|------|------|
| 豆包上传图片 | ✅ 通过 | 图片上传成功 |
| 豆包获取回复 | ✅ 通过 | 能获取完整回复 |
| 提示词提取 | ✅ 通过 | 成功提取5组 |
| Legil 输入提示词 | ✅ 通过 | 输入正常 |
| Legil 点击生成 | ✅ 通过 | 按钮点击成功 |
| Legil 生成检测 | ✅ 通过 | 正确等待生成完成 |
| Legil 图片保存 | ⏳ 待验证 | 需进一步测试 |
| 完整端到端流程 | ⏳ 未测试 | 明日进行 |

---

## 回滚计划

如需回滚今日变更，关键版本点：

| 版本 | 状态 | 回滚命令 |
|------|------|----------|
| 添加截图功能前 | 可用 | `git checkout HEAD~3 -- legil-automation.js` |
| 优化提取算法前 | 可用 | `git checkout HEAD~1 -- doubao-automation.js` |

---
*记录时间: 2026-05-07*
