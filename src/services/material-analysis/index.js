const { createMaterialAnalysisService } = require('./importer');
const { createMaterialKnowledgeCollectorService } = require('./knowledge-collector');

module.exports = {
    createMaterialAnalysisService,
    createMaterialKnowledgeCollectorService
};
